import 'package:flutter/material.dart';

class OfferTypeDropdown extends StatelessWidget {
  const OfferTypeDropdown({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'this is text',
    );
  }
}
