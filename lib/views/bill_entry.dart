import 'package:flutter/material.dart';

class BillEntry extends StatelessWidget {
  const BillEntry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Bill Entry Page');
  }
}
